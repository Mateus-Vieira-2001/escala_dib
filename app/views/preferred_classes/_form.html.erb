<%= form_with(model: preferred_class) do |form| %>
  <% if preferred_class.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(preferred_class.errors.count, "error") %> prohibited this preferred_class from being saved:</h2>

      <ul>
        <% preferred_class.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_field :description %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
