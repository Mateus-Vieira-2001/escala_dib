<%= form_with(model: preferred_day) do |form| %>
  <% if preferred_day.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(preferred_day.errors.count, "error") %> prohibited this preferred_day from being saved:</h2>

      <ul>
        <% preferred_day.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_field :description %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
